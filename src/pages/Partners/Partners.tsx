import { Grid } from '@mui/material';
import Header from 'components/Header';

const Partners = () => {
  return (
    <Grid>
      <Header />
      <span>partners</span>
    </Grid>
  );
};

export default Partners;
