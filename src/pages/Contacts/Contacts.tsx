import { Grid } from '@mui/material';
import Header from 'components/Header';

const Contacts = () => {
  return (
    <Grid>
      <Header />
      <span>Контакты</span>
    </Grid>
  );
};

export default Contacts;
